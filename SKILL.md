---
name: minecraft-modpack-upstream-sync
description: Use when a customized Minecraft modpack must follow an author's pack update while preserving an existing world, local additions or patches, especially across Fabric and NeoForge or after an earlier full-pack port.
---

# 定制整合包同步作者更新

目标是同步作者的玩法与内容契约，同时保留用户选择的本地功能。作者版本号、文件数量、启动成功都不能单独证明同步完成。

这是 AI 的操作指南与只读工具集，不是自动更新器。优先使用目标加载器的原生对应模组；目标是玩法尽可能一致，不是把参考包所有文件强行移植。完整重写模组不属于默认范围。只有根因明确、修改面小、能复现和验证时才考虑局部适配；否则保留原包玩法并列出暂缓项。

## 先确定当前阶段

沿用用户已给的范围和授权。用户说“先检查，测试稍后”时，只读实例并在独立工作目录产出清单和适配材料；不要复制正在保存的世界、启动游戏、改主实例或启动器数据库。检查结果有未解决适配项时，明确写“尚非只剩测试”。

安装、操作桌面、进入存档、发布文件分别按用户实际授权处理；后续明确许可可以改变此前限制，不要反复索取已给的许可。上下文或历史记录不完整时，从当前文件、清单与日志恢复状态，不能因旧摘要停留在上一批就重做或覆盖已应用内容。

## 1. 找到真实基线

- 核对启动器显示名、实际目录、最新启动日志的 gameDir、MC 与加载器精确版本。目录名和旧台账可能过期。
- 检索之前移植的台账、备份清单、最终交付及后续用户反馈。把实际修复、失败尝试、已撤回补丁分开；不能把旧回复的“修好了”当实机证据。
- 建立三个来源：作者旧版 B、作者新版 U、当前定制版 L。混合包记录多个祖先（例如 NeoForge 基座 + 后来移入的 Fabric 内容），不要伪造一个完整共同基线。
- 读作者更新日志中的删除、迁移、改名、依赖联动；不以“更新全部模组”代替作者版本同步。

## 2. 生成差异证据

用 [只读比较工具](scripts/compare_pack.py)；先看 `--help`。它接受原始 Modrinth ZIP 和实际实例目录，记录文件哈希、ZIP 成员差异与三方状态。解压后的导入包目录、普通实例 ZIP、空输入不受支持，会报错；不要将其当成作者删除全部内容。Python 3.11+，无第三方依赖、不下载、不安装。

```text
python scripts/compare_pack.py --base author-old.zip --upstream author-new.zip --local current-instance --out audit/three-way.json
```

工具是**路径比较**，不是安装清单。JAR 改名、Fabric/NeoForge 替代、远程 index 引用不能按“文件不存在”判成缺模组。世界文件不在工具范围，另行只读核对。

输出必须是所有输入之外的新文件，重复运行换一个报告文件名。索引中的客户端/服务端 `env` 条件会保留；可选或禁用条件标记 `unresolved`，由明确的使用侧别解决。分端 overrides 暂不支持，不能把服务端专用文件当客户端安装候选。

| 差异 | 后续决策 |
|---|---|
| L 与 U 相同 | 保留；ZIP 只改变压缩封装不必更新 |
| U 与 B 相同，L 不同 | 保留本地改动；本地删除也是改动 |
| L 与 B 相同，U 不同 | 更新候选，仍需依赖和玩法验证 |
| 两边都改 / 基线不明 | 冲突，读实际字段或包内条目 |
| 作者删除 | 查功能迁移和旧档引用；不自动删除，也不把旧内容机械留在高优先级覆盖新版 |

补足人工证据：活动及禁用 JAR、模组 ID/版本/依赖、声明的嵌套 JAR、配置、KubeJS、资源与光影顺序、全局包入口、实际世界 `level.dat` 的数据包启用顺序及世界内补丁。读取副本或稳定落盘状态，记录观察时间。

## 3. 按功能链解决适配

按 [兼容性与历史失败判读](references/compatibility.md) 核查：

- 匹配 MC + 模组 ID + 加载器 + 版本范围 + 文件哈希；同名同版本也可能是作者改版。区分顶层重复与加载器选取的嵌套依赖。
- 优先核查作者目标版本的原生构建；只在缺少原生版时评估现有桥接层。Connector 能识别 JAR 不等于能正确运行其 Mixin、注册和事件。
- 查历史失败**条件**。只有目标、依赖、数据或补丁条件发生可解释变化，才重新验证；不永久拉黑已经换了条件的版本，也不跳过仍适用的失败。
- 保留本地玩法与修复；不要为了配平依赖擅自降级底座、删功能、清世界数据，或修改元数据假装兼容。
- 任务按章节/任务/条件/奖励的 ID 与引用合并。合并本地科技章节和作者新章节，检查 chapter group、依赖边、物品及命令；ID 数不是任务数，重复 ID 要列冲突。
- 数据包与资源包成组迁移，检查同一资源的有效提供者。KubeJS 以配方/注册 ID 为边界；Node 语法检查不能代替 Rhino 与游戏注册表验证。

专项指南按需读取：

- FTB 老章节流程优化、重编号、删除条件与奖励、DLC 提供者漏查：[任务流程同步](references/quest-flow.md)。保留用户进度不等于保留作者已淘汰的旧逻辑。
- Cobblemon `species_additions` 二次覆盖、硬币/皮肤、模型查询差异、Rhino 和构建工具返修：[运行时与返修](references/runtime-and-repair.md)。磁盘内容、资源栈胜出、最终解析对象、实际玩法四层分别取证。

任务草稿可调用 [SNBT 合并模块](scripts/merge_quests.py) 的 `parse`、`merge(B, U, L, notes)`、`dump`；这是无文件写入的 Python 模块，没有安装命令。调用者负责将输出写到独立新目录，保留 `notes` 并对解析结果往返校验。缺失文件用模块的 `MISSING` 表示；返回该值时不生成文件。默认拒绝重复键；只有证实实际 FTB 解析器采用后值覆盖时，才显式传入重复项记录列表和 `last_wins=True`。不能对全部来源统一放宽。

该模块保守保留本地 ID、删除意图和冲突字段，**不会检查任务依赖图、物品注册、命令或功能等价**。调用者还须比较合并前后的失效引用；文件名不是任务依赖。章节换 ID、本地与上游同时新增同 ID、上游删除旧对象等情况需要人工审查，不能把可解析草稿直接安装。

用户要求设置不变时，记录并逐项比较全部已有按键、资源包启用列表及顺序、光影和配置；游戏退出后刷新快照，避免覆盖刚保存的设置。新功能的默认按键若与原键位冲突，只处理新增键位。不要通过重置选项或自动启用作者的全部资源包实现同步。

## 4. 产出可复现适配清单

每项记录：功能链、B/U/L 来源与校验值、目标来源/下载与哈希、操作及理由、依赖、保留的本地意图、冲突解决、历史失败条件、验证状态与回滚范围。将 `candidate`、`blocked`、`static_checked`、`runtime_passed` 分开。

先按功能链归入低风险数据适配、有持续兼容风险、缺提供者或大规模重写而暂缓三类；风险类别不等于测试通过。同链依赖闭合后集中测试一批，别让用户每替换一个文件就进档。若缺对话/训练师桥接，相关 DLC 同组隔离，不能只移除桥接却保留“DLC 可用”的结论。

自制补丁记录输入哈希、源码/变更、构建命令、输出哈希、对应失败与退出条件。没有这些资料的旧补丁标记待调查，不能称可复现。每个新尝试从该链干净基线重建；不在失败产物上连续堆补丁。

## 5. 在用户允许时测试与应用

开始时用启动器记录和实际 gameDir 绑定用户所指主实例的显示名、稳定 ID 与绝对路径；文件夹叫 Full Port 或 TEST 不能单独证明角色，显示名与目录名可能完全不同。固定该绑定，后续测试/导出副本不得悄悄升格为 MAIN。完成后将已验收内容按清单同步回该主实例，导出也只从同步并核对后的主实例生成。若用户实际在副本中继续正式游玩，先明确最新进度归属，保全关联存档；不能将带测试道具的世界直接覆盖正式进度。

全流程收尾包含释放磁盘空间：代理能完成且已通过的测试，确认其内容已归入主实例、无独有进度后清理对应副本；仍需人工验收的副本注明具体待测项并保留。用户明确表示已试玩接受、未来问题另修时记录为用户验收，不要求无限穷举或永久保留全部 TEST，也不伪造未测项通过。所有必要测试完成或用户明确接受剩余范围后，核对主实例与最终清单，再删除废弃 TEST、重复暂存和失败导出；清理前解析精确路径、检查占用和独有数据，保留必要回滚资料与简明证据，核对删除结果及释放字节。启动器登记同步清理，不能留下失效实例入口。

确认游戏正常退出，创建独立测试副本。先验证未更新基线，再测试依赖闭合的功能链，最后组合回归。测试真实旧世界和对应玩家身份；离线新账号无法证明原玩家队伍、PC、任务和背包完好。

已有基线或功能证据若精确输入、相关配置与触发条件未变，可以复用并引用；受影响链或最终组合改变才重测对应范围，不每次从零跑全包。

按 [集中验收与发布](references/acceptance.md) 设计全量静态检查加代表性运行测试。不要默认逐款穷举数千皮肤；先测不同提供者、交互与存档机制。用户选择跳过世界测试时记录该限制，不能把菜单通过改写为旧档通过。

按本批影响范围核对旧宝可梦与形态/道具、PC、对战、道馆模式、任务领奖、商店货币、配方、骑乘、旧建筑/机器、目标资源与光影；保存退出后重进。不是每次都全包穷举。检测到未计划的区域删除、注册丢失或数据重置就停止该链。正常保存会改文件，应比较数据语义，不能要求世界逐字节不变。

主实例应用只沿用明确授权的范围，采用测试过的精确文件与顺序，先备份替换项；涉及数据迁移时还要备份关联世界/玩家/地图数据。应用前复核主实例基线哈希，漂移则重新比对。不能将测试世界反向覆盖主世界。

朋友补丁须携带目标与原文件哈希、精确退役文件清单和回滚说明；不同基线先预检，不能靠解压覆盖遗留旧 JAR。不要发布个人世界、玩家资料、账户数据、启动参数令牌或调试缓存。

报告分别说明已检查、已准备、TEST 已测试、MAIN 已应用及 MAIN 已验证范围。内容已应用可以明确说“本批已安装”；TEST 世界通过、MAIN 仅菜单通过时明确区分。不能把局部通过写成“作者全部更新完成”“全部皮肤可用”或“旧档完整稳定”。技能本身不会替代真实运行、自动获得权限或保证零故障。
